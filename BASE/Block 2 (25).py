"""
Контейнеры + функции
25 задач от нейронки + практика с Notion
Затраченное время: 1 час.

... Я нихрена не понимаю.
"""

"""
1.
Уникальные элементы с сохранением порядка
Напиши функцию unique_preserve(lst), которая принимает список и возвращает новый список,
содержащий только уникальные элементы, сохраняя порядок их первого появления.
Пример: [1, 2, 2, 3, 1, 4] → [1, 2, 3, 4]
"""

some_list_1 = [1, 2, 2, 3, 1, 4]
some_list_2 = ["Alfa", 1, 3, 2, 4, 3, 1, "Beta", 5, 7, 9, 22, 4, "Gamma"]
some_list_3 = [3, 3]


def unique_preserve(lst: list) -> list:
    """
    Принимает список.
    Возвращает список из уникальных элементов с сохранением порядка.
    :param lst: Список для сортировки.
    :return: Новый список с уникальными элементами и сохранением порядка.
    """
    unique_list = []
    for item in lst:
        if item not in unique_list:
            unique_list.append(item)
    return unique_list


"""
2.
Форматирование списка в строку
Напиши функцию format_list(items), которая принимает список строк и возвращает строку,
где элементы перечислены через запятую, а последний соединяется словом "и".
Пример: ['яблоки', 'бананы', 'груши'] → 'яблоки, бананы и груши'
"""

some_list_4 = ['яблоки', 'бананы', 'груши']
some_list_5 = ['яблоки', 'бананы', 'груши', "ананасы", "мандарины", "персики"]
some_list_6 = ['яблоки']


def format_list(lst: list) -> str:
    """
    Форматирует строку из списка, добавляя "и" перед последним элементом.
    :param lst: Список элементов.
    :return: Отформатированная строка.
    """
    if len(lst) > 1:
        return ', '.join(lst[:-1]) + f' и {lst[-1]}'
    else:
        return lst[0]


"""
3.
Средний балл студентов
Дан список словарей:
students = [{'name': 'Анна', 'age': 20, 'grades': [4, 5, 3]}, ...]
Напиши функцию avg_grades(students), которая возвращает список словарей,
дополненных ключом 'avg' (средняя оценка), и сортирует студентов по убыванию среднего балла.
"""

students = [
    {'name': 'Анна', 'age': 20, 'grades': [4, 5, 3]},
    {'name': 'Иван', 'age': 21, 'grades': [5, 5, 5, 5]},
    {'name': 'Мария', 'age': 19, 'grades': [3, 4, 4, 3, 5]},
    {'name': 'Петр', 'age': 22, 'grades': [2, 3, 3, 4]},
    {'name': 'Елена', 'age': 20, 'grades': [5, 4, 5, 4, 5]},
    {'name': 'Дмитрий', 'age': 23, 'grades': [3, 3, 2]},
    {'name': 'Ольга', 'age': 20, 'grades': [4, 4, 4, 4]},
    {'name': 'Алексей', 'age': 21, 'grades': [5, 5, 5]},
]


def avg_grades(stud: list) -> list:
    result = []
    for student in stud[0:]:
        avg = round(sum(student.get('grades')) / len(student.get('grades')))
        student.update({'avg': avg})
        result.append(student)
    result.sort(key=lambda x: x['avg'], reverse=True)
    return result


if __name__ == '__main__':
    print(unique_preserve(some_list_3))
